const initState = {
    categories: [],
    category: '1',
    difficulty: '',
}

const triviaReducer = (state = initState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

export default triviaReducer;